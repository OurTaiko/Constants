# OurTaiko 定数算法

> <https://github.com/OurTaiko>

## 本地运行配置

快速开始见 [README](../README.md)。以下命令均在仓库根目录执行。

首次运行将 `.env.example` 复制为仓库根目录的 `.env`，把 `SONGS_BASE_DIR` 改为本机谱面目录，然后执行 `uv run src/batch_workflow.py`。

路径配置优先级为：`--base-dir` 参数 > 系统环境变量 `SONGS_BASE_DIR` > `.env`。未配置时程序会提示设置路径。`.env` 和 `.env.*` 已被 Git 忽略，仅提交 `.env.example` 配置示例。

### 一次运行完成计算和导出

```sh
uv run src/batch_workflow.py
```

同时生成 `raw_constants.json`（完整结果、校准值和错误信息）与 `constants.json`（edit/oni/hard 精简结果），无需再手动运行 extract。所有难度和分支仍参与原有全局校准，公式和选谱规则不变。

- `--limit 5`：小批量测试，默认输出 `raw_constants.sample.json` 和 `constants.sample.json`，不覆盖正式结果。测试使用子集校准，定数不能与全量直接比较。
- `--output out/raw.json`：指定完整结果路径，精简结果默认写入同目录的 `constants.json`；小批量时为 `constants.sample.json`。
- `--constants-output out/constants.json`：单独指定精简结果路径。显式路径会覆盖对应文件，请避免将测试输出指向正式文件。
- `--no-cache`：不读取也不写入任何缓存。
- `--refresh`：重新请求并更新本次处理歌曲的两层缓存，不删除整个目录，不能与 `--no-cache` 同用。

缓存分两层：`.cache/{id}.json` 保存 API 分析；`.cache/raw/{id}.json` 保存原始指标。旧 API 缓存可继续使用，首次运行补建指标缓存。原始分析和 algorithms 代码变化时自动重新计算指标；仅修改 `rating.py` 时复用指标，仍对全部成功谱面重新校准和计算最终定数。服务端在同一 URL 更新时，可使用 `--refresh` 强制更新。

独立 `uv run src/extract_song_constants.py` 仍可用于重新导出已有 raw 数据。输出文件分别原子替换，但两份文件并非跨文件事务；若第二份写入失败，可用独立 extract 恢复。部分歌曲失败时仍按原流程输出成功部分，详情见 raw 的 `errors`。

离线回归测试：`uv run python -m unittest discover -s Tests -p "test_*.py"`。

## 初始数据

D: totalNotes  
E: stamina  
F: speed  
G: burst  
H: complex  
I: complexRatio  
J: rhythm  
K: rhythmRatio  

## 换算数据

L: 体力换算=MAX(MIN(16.3783/(1+EXP(-0.6764×(stamina-7.2836)))-0.6012,15.5),0)  
M: 手速换算=IF(speed<5,3×POWER(speed/5,35/3),IF(speed<6,7×speed-32,IF(speed<10,11/8×speed+1.75,15.5)))  
N: 爆发换算=IF(burst<35.5/7,POWER(14×burst/71,71/2),IF(burst<6,7×burst-34.5,IF(burst<10,7/4×burst-3,IF(burst<12.75,(8×burst+239)/22,15.5))))  
O: 复合占比换算=18200.736×TANH(4.491×complexRatio+3.86)-18184.558
P: 复合上限=17.7743/(1+EXP(-0.0083×totalNotes+2.8484))-0.9613  
Q: 复合换算=MIN((复合占比换算-MIN(所有乐曲的复合占比换算))/(MAX(所有乐曲的复合占比换算)-MIN(所有乐曲的复合占比换算))×15.5,复合上限)  
R: 节奏占比换算=20.1353/(1+EXP(-18.0625×(rhythmRatio-0.0692)))-4.4496  
S: 节奏上限=17.4097/(1+EXP(-0.007×totalNotes+2.7059))-1.0787  
T: 节奏换算=MIN(节奏占比换算,节奏上限)

## 根据换算计算各维度

U: 体力=(体力换算-MIN(所有乐曲的体力换算))/(MAX(所有乐曲的体力换算)-MIN(所有乐曲的体力换算))×15.5  
V: 手速=手速换算/MAX(所有乐曲的手速换算)×15.5  
W: 爆发=爆发换算/MAX(所有乐曲的爆发换算)×15.5  
X: 复合=(复合换算-MIN(所有乐曲的复合换算))/(MAX(所有乐曲的复合换算)-MIN(所有乐曲的复合换算))×15.5  
Y: 节奏=(节奏换算-MIN(所有乐曲的节奏换算))/(MAX(所有乐曲的节奏换算)-MIN(所有乐曲的节奏换算))×15.5

## 75定数

Z: 粗糙75定数=SQRT(SUMSQ(体力,手速,复合)/3)  
AA: 归一75定数=15.5×粗糙75定数/MAX(所有乐曲的粗糙75定数)  
AB: 定数粗略值=SQRT((SUMSQ(体力,手速,爆发,复合,节奏)-0.9×MIN(体力,手速,爆发,复合,节奏)×MIN(体力,手速,爆发,复合,节奏))/4.1)

## 主定数权重

AC: 体力权重=IF(MIN(体力,手速,爆发,复合,节奏)=体力,0.1,0.7×(1-体力条件判断)+0.3)  
AD: 手速权重=IF(MIN(体力,手速,爆发,复合,节奏)=手速,0.9×手速条件判断+0.1,1)  
AE: 爆发权重=IF(MIN(体力,手速,爆发,复合,节奏)=爆发,0.1,0.9×爆发条件判断+0.1)  
AF: 复合权重=IF(MIN(体力,手速,爆发,复合,节奏)=复合,0.1,0.9×(1-复合条件判断)+0.1)  
AG: 节奏权重=IF(MIN(体力,手速,爆发,复合,节奏)=节奏,0.1,0.9×(1-节奏条件判断)+0.1)

## 比较值参数

AH: 平均Minus10=0.5×TANH(1×(定数粗略值-10))+0.5  
AI: 体力Minus平均=0.5×TANH(3×(体力-定数粗略值+0.5))+0.5  
AJ: 体力Minus14.5=0.5×TANH(3×(体力-14.5))+0.5  
CO: 体力Minus14=0.5×TANH(3×(体力-14))+0.5  
CP: 体力Minus13.5=0.5×TANH(3×(体力-13.5))+0.5  
CQ: 手速Minus11=0.5×TANH(3×(手速-11))+0.5  
CR: 爆发Minus15=0.5×TANH(3×(爆发-15))+0.5  
CS: 爆发Minus8.5=0.5×TANH(3×(爆发-8.5))+0.5  
CT: 节奏Minus主定数=0.5×TANH(3×(节奏-归一主定数))+0.5  
AK: 爆发Minus平均=0.5×TANH(3×(爆发-定数粗略值+0.5))+0.5  
AL: 复合Minus平均=0.5×TANH(3×(复合-定数粗略值+0.5))+0.5  
AM: 节奏Minus平均=0.5×TANH(3×(节奏-定数粗略值+0.5))+0.5

## 条件判断

AN: 体力条件判断=平均Minus10×体力Minus平均×(1-体力Minus14.5)  
AO: 手速条件判断=平均Minus10  
AP: 爆发条件判断=1-爆发Minus平均  
AQ: 复合条件判断=平均Minus10×复合Minus平均  
AR: 节奏条件判断=平均Minus10×节奏Minus平均

## 主定数

AX: 粗糙主定数=SQRT(SUMPRODUCT((体力^2,手速^2,爆发^2,复合^2,节奏^2),(体力权重,手速权重,爆发权重,复合权重,节奏权重))/SUM(体力权重,手速权重,爆发权重,复合权重,节奏权重))  
AY: 归一主定数=IF(粗糙主定数>13.3,13.3+(15.5-13.3)×(粗糙主定数-13.3)/(MAX(所有乐曲的粗糙主定数)-13.3),粗糙主定数)  
CN: 主定数范围=IF(归一主定数>14.5,1,IF(归一主定数>13.5,2,IF(归一主定数>12.5,3,IF(归一主定数>11.5,4,IF(归一主定数>9.5,5,6)))))

## 99定数权重

BE: 体力99定数权重=SWITCH(主定数范围,3,1×体力Minus14+体力权重×(1-体力Minus14),4,1×体力Minus13.5+体力权重×(1-体力Minus13.5),5,0.1,6,0.1,体力权重)  
BE: 手速99定数权重=SWITCH(主定数范围,5,0.5×手速Minus11+0.5,6,0.9×手速Minus11+0.1,手速权重)  
BG: 爆发99定数权重=SWITCH(主定数范围,1,1,2,0.5,3,0.5×爆发Minus15+爆发权重×(1-爆发Minus15),4,0.5,5,0.3×爆发Minus8.5+0.5,6,0.9×爆发Minus8.5+0.1,爆发权重)  
BH: 复合99定数权重=SWITCH(主定数范围,1,复合权重,2,复合权重,0.1)  
BI: 节奏99定数权重=SWITCH(主定数范围,1,0.3,2,0.5,3,0.5,4,0.8,5,0.5×节奏Minus主定数+0.3,6,1,节奏权重)

## 99定数

BO: 粗糙99定数=SQRT(SUMPRODUCT((体力^2,手速^2,爆发^2,复合^2,节奏^2),(体力99定数权重,手速99定数权重,爆发99定数权重,复合99定数权重,节奏99定数权重))/SUM(体力99定数权重,手速99定数权重,爆发99定数权重,复合99定数权重,节奏99定数权重))  
BP: 归一99定数=IF(粗糙99定数>13.3,13.3+(15.5-13.3)×(粗糙99定数-13.3)/(MAX(全部乐曲的粗糙99定数)-13.3),粗糙99定数)  
BQ: 手速限制=1/8×手速×手速+10  
BR: 最终99定数=MIN(归一99定数,手速限制)

## 最终定数

DQ: sub_constant_1=归一75定数  
DR: main_constant=归一主定数  
DS: sub_constant_2=最终99定数  
DT: stamina=体力  
DU: handspeed=手速  
DV: burst=爆发  
DW: complex=复合  
DX: rhythm=节奏
